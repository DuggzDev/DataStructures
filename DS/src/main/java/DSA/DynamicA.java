package DSA;

import java.util.ArrayList;

public class DynamicA {
    //Dynamicarray = arraylist
    DynamicA(){
        ArrayList<Integer> Numbers = new ArrayList<>();

        //Capacity is how much the array HOLDS
        //size is how many elements are not null within the array
        Numbers.add(30);
        Numbers.size();
        Numbers.forEach(System.out::println);

    }
}
